# Railway/Nixpacks monorepo configuration.
# Keeps frontend and backend separate and deploys backend service from /backend.

[variables]
NIXPACKS_NODE_VERSION = "20"

[phases.install]
cmds = ["cd backend && npm ci"]

[start]
cmd = "cd backend && npm start"
